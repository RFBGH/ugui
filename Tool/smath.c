#include "smath.h"

#if 1
INT MAX(INT x, INT y)
{
    if(x > y)
    {
        return x;
    }
    return y;
}

INT MIN(INT x, INT y)
{
    if(x < y)
    {
        return x;
    }
    return y;
}
#endif

